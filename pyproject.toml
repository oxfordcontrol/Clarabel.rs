[build-system]
requires = ["maturin>=0.13,<0.15"]
build-backend = "maturin"

[project]
name = "clarabel"
requires-python = ">=3.7"
dependencies = [
    "numpy",
    "scipy"
] 
classifiers = [
    "Programming Language :: Rust",
]

[tool.maturin]
python-source = "python"